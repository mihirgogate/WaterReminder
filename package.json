{
  "name": "WaterReminder",
  "version": "1.0.0",
  "description": "Water Reminder on Mac",
  "dependencies": {
    "config": "^1.20.4",
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.36.0",
    "cached-tape": "^2.0.0"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Example --platform=darwin --arch=x64 --version=0.36.0",
    "start": "electron ."
  }
}
